CREATE TABLE nurses (
    id                 int           primary key,
    first_name         varchar(100),
    last_name          varchar(100),
    phone              varchar(15),
    email              varchar(100)
);